{
  "name": "authorx",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "nearleyc ./grammar.ne > grammar.js",
    "readme": "npm run build && cat README.ax | node parse.js > README.ast.json && node code-gen.js README.ast.json > README.md",
    "test": "npm run build && jest"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "array-flat-polyfill": "^1.0.1",
    "nearley": "^2.19.6"
  },
  "devDependencies": {
    "jest": "^26.4.2"
  }
}
