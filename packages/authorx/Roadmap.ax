<l {  
  << Add composition-based api (to make it easy to extend markdown-like semantics, for example)
  << Bracket-less functions for single lines  
  << Quoting? (for embedding code within .ax docs)
  << Buffer tweets example semantics?
  << Slack onboarding example semantics?
  << Java bindings
  << R bindings?
  << Custom lexing flags to cli?
  << Split parser from js bindings?
  << reveal.js presentation example semantics?
}