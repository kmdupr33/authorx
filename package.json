{
  "name": "root",
  "private": true,
  "scripts": {
    "readme": "lerna run build && packages/compiler/bin/compile.js docs/index.ax ax-markdown.js > docs/index.md",
    "publish": "lerna run build && npm test && lerna publish",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
